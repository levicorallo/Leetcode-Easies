class Solution:
    def isPalindrome(self, x: int) -> bool:
        if str(x) == str(x)[::-1]: #[::-1]: is a command to grab the reverse e.g. racecar to racecar 
            return True
        else:
            return False
